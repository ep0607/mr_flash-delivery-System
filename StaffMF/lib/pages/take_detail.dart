import 'package:flutter/material.dart';


class TakeDetail extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Take Detail'),
      ),
      body: Container(
        child: Text('I am Take'),
      ),
    );
  }

}